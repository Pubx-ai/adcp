{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/schemas/curation/create-deal-async-response-input-required.json",
  "title": "Create Deal - Input Required",
  "description": "Payload when create_deal task is paused waiting for user input or approval.",
  "type": "object",
  "properties": {
    "reason": {
      "type": "string",
      "enum": [
        "APPROVAL_REQUIRED",
        "PLATFORM_CONFIRMATION"
      ],
      "description": "Reason code indicating why input is needed"
    },
    "errors": {
      "type": "array",
      "description": "Optional validation errors or warnings for debugging purposes. Helps explain why input is required.",
      "items": {
        "$ref": "/schemas/core/error.json"
      }
    },
    "context": {
      "$ref": "/schemas/core/context.json"
    },
    "ext": {
      "$ref": "/schemas/core/ext.json"
    }
  },
  "required": ["reason"],
  "additionalProperties": true
}
