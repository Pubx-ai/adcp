{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/schemas/curation/update-deal-async-response-input-required.json",
  "title": "Update Deal - Input Required",
  "description": "Payload when update_deal task is paused waiting for user input or approval.",
  "type": "object",
  "properties": {
    "reason": {
      "type": "string",
      "enum": [
        "APPROVAL_REQUIRED",
        "CHANGE_CONFIRMATION"
      ],
      "description": "Reason code indicating why input is needed"
    },
    "context": {
      "$ref": "/schemas/core/context.json"
    },
    "ext": {
      "$ref": "/schemas/core/ext.json"
    }
  },
  "required": ["reason"],
  "additionalProperties": true
}
